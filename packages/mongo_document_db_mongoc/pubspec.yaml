name: mongo_document_db_mongoc
description: MongoDB driver for Dart backed by the official MongoDB C Driver (libmongoc) via FFI.
version: 1.7.30
homepage: https://github.com/wanclem/mongo_document
repository: https://github.com/wanclem/mongo_document
issue_tracker: https://github.com/wanclem/mongo_document/issues

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  ffi: ^2.1.3
  meta: ^1.16.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
